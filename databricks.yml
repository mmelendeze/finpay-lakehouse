# This is a Databricks asset bundle definition for Bundle_20260525.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Bundle_20260525
  uuid: b531fce8-3820-4a04-9985-be5738499962

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  schema_event_log:
    description: The schema to use
  schema_gold:
    description: The schema to use
  sql_warehouse_id:
    description: The schema to use
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-35f7e007-14f2.cloud.databricks.com
    variables:
      catalog: fintech_finpay
      schema: bronze
      schema_event_log: observability
      schema_gold: gold
      sql_warehouse_id: 7fb6e91c39c8a816
  prod:
    mode: production
    workspace:
      host: https://dbc-35f7e007-14f2.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/mmelende@hotmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/mmelende@hotmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: fintech_finpay
      schema: bronze
      schema_event_log: observability
      schema_gold: gold
      sql_warehouse_id: 7fb6e91c39c8a816
    permissions:
      - user_name: mmelende@hotmail.com
        level: CAN_MANAGE
